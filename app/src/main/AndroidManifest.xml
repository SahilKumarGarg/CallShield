<!-- File: android/app/src/main/AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sahil.callshield">

    <!-- 
      Zero Invasive Permissions Policy:
      NO android.permission.READ_CALL_LOG
      NO android.permission.READ_PHONE_STATE
      NO android.permission.INTERNET (100% Offline Guarantee for Play Store approval)
    -->

    <!-- Battery optimization exemption for persistent call screening -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 
      Required by Android Telecom Framework to screen calls from saved contacts.
      By default, Android automatically bypasses CallScreeningService for any number 
      present in the user's phonebook unless READ_CONTACTS is declared and granted.
    -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CallShield">

        <!-- 
          CWE-280 Mitigation & Android RoleManager Requirement:
          android:permission="android.permission.BIND_SCREENING_SERVICE"
          Ensures ONLY the Android System Telecom framework (UID 1000) can bind to this service.
          This exact permission is required by Android for the app to qualify and appear
          under "Default Caller ID & spam app" in system settings.
        -->
        <service
            android:name=".service.CallFilterScreeningService"
            android:permission="android.permission.BIND_SCREENING_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
